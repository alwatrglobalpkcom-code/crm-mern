{
  "name": "crm-production",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "install:server": "cd server && npm install",
    "start": "cd server && npm install --omit=dev && node index.js",
    "setup": "cd client && npm install && npm run build && cd ../server && npm install"
  },
  "engines": {
    "node": ">=18.x"
  }
}
